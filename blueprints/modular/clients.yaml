extends@: default

form:
  fields:
    tabs:
      fields:
        content:
          fields:
            header.clients:
              type: list
              label: THEME.ADMIN.CLIENTS
              fields: 
                .image: 
                  type: pagemediaselect
                  label: THEME.ADMIN.IMAGE
                  preview_images: true
                .alt: 
                  type: text
                  label: THEME.ADMIN.ALT
               